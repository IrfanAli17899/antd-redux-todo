export interface ITodo {
    id: number;
    title: string;
    description: string;
    is_completed: boolean;
}

export interface IUser {
    id: number;
    username: string;
    // password: string;
}